/**
 * Client-side search
 *
 * Note: search.js is not exported through TypeScript as it is used
 * as a separate file for web worker in the browser
 */

// Export paths to client files
export const CLIENT_SEARCH_PATH = './search.js';